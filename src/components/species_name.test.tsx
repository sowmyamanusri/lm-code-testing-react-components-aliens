import SpeciesName , {SpeciesNameProps}from './species_name';
import {render, screen, fireEvent} from '@testing-library/react';

describe('SpeciesName component',()=>{
    test(`when component is rendered,
    `)
})

